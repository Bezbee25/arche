# arche — orchestrateur de développement multi-plan
# Pas de librairie LLM Python : arche appelle les CLIs (claude, gemini, codex) via subprocess.

# CLI
typer>=0.9.0,<1.0.0

# Terminal UI
rich>=13.7.0,<14.0.0
questionary>=2.0.0

# Web server + WebSocket
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
websockets>=12.0

# Terminal web (WebSocket → pty)
ptyprocess>=0.7.0

# Storage
pyyaml>=6.0.1

# Compatibilité FastAPI (formulaires/multipart optionnel)
python-multipart>=0.0.9

# Data validation
pydantic>=2.0.0,<3.0.0
